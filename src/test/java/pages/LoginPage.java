package pages;

public class LoginPage {
    // rename it as you want and delete comment later
    //place all locators you will need here @FindBy
    //and after done with that create constructor (same name as class name)
    // use => PageFactory.initElements(driver, this) to initialize constructor
    // and make sure login page name matches in pageInitializer
    //please delete this is only review
}
